import React from 'react';

import { StudentAverageLog } from './components'
import './App.css';

export const App = () => {
  return (
    <StudentAverageLog />
  );
}

// TODO
// Create a table with 3 columns: 'First Name', 'Last Name', 'Exam Score'

// Create a form to add a student and a score (2 text inputs, 1 number input).

// Add some students.

// As you add the students, log the average exam score for all of the students - 'a class average.'

// If the 'class average' is below 70, apply some type of 'special Bulma text class.'

// Do the same thing with any individual student - if score is below 70...
